@{
    ViewData["Title"] = "Actors Page";
}
@model List<Actor>

<div class="text-center">
    @if (Model.Count > 0)
    {
        <h1>Feature list</h1>
        <a class="btn btn-primary" href="@Url.Action("Create", "Actor")">Inserisci un/una nuovo/a attore/attrice</a>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col"> Surname</th>
                    <th scope="col">Azioni</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <th scope="row">@item.Id</th>
                        <td>@item.Name</td>
                        <td>@item.Surname</td>
                        <td>
                            <a class="btn btn-primary" href="@Url.Action("Update","Actor", new {id = item.Id})">Update</a>
                            <form asp-action="Delete" asp-controller="Actor" asp-route-id="@item.Id" method="post">
                                <button class="btn btn-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <h1>La tabella Actors e' vuota</h1>
        <a class="btn btn-primary" href="@Url.Action("Create", "Actor")">Inserisci un/una nuovo/a attore/attrice</a>
    }
</div>